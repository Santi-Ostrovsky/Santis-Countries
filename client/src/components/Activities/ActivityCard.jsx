import React from "react";

export default function ActivityCard({ name }) {
  return (
    <>
      <h3>{name}</h3>
      {/* <img src={flag} alt={`${name} flag`} />
      <h5>{continent}</h5> */}
    </>
  );
}
